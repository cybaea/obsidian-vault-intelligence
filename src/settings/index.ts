// src/settings/index.ts

// Re-export the Interface and Defaults so main.ts can find them
export * from './types';

// Re-export the Class so main.ts can instantiate the tab
export * from './settingsTab';
